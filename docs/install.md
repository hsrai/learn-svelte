# Instllation

Use `npm create vite@latest` is install plain Svelte and use `npx sv create myapp` for
SvelteKit.


```sh
npm create vite@latest

> npx
> create-vite

│
◇  Project name:
│  learn-svelte
│
◇  Select a framework:
│  Svelte
│
◇  Select a variant:
│  TypeScript
│
◆  Install with npm and start now?
│  ● Yes / ○ No
```

